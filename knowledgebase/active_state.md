# Active State (RAM)
*This file is strictly maintained by the Master Assistant to track active priorities.*

## 🎯 Current Priorities
- **Project Structure**: Active testing of the Browser Framework. 
- **Browser Subagent Test**: Successfully deployed the subagent under strict kill-switch constraints to autonomously generate a "Cinematic UFO" via Google Flow while perfectly respecting Human-In-The-Loop authentication boundaries.
- **Video Downloader Integration**: Integrated native Standalone YouTube Terminal Downloader natively into the framework executing `yt-dlp` explicitly saving to `/DOWNLOADS` following dynamic `V-ID` versioning conventions.
- **Multicam Workflow Prep**: Preparing to structurally transfer the Whisper Transcriber and XML Word-level XML intelligent cut framework from the *Bronson Cancer Equity* project archive into `MASTER V1` functionality.

## 📌 Background Jobs & Tracking
- **Warrants (DAAD)**: Paused for now, maintaining awareness.
- **Flight Prices**: Tracking Allegiant/Michigan flights. Phase 4 will handle async monitoring.

## ⚠️ Known Bugs / Bottlenecks / Fixes
- **Unified Profile Execution**: `amos_browser.py` was structurally modified to map directly to `~/.gemini/antigravity-browser-profile` instead of creating an isolated `sessions` folder. This guarantees that background automated processes use the active Native cookies, dramatically lowering detection risks and authentication walls. 
- **Raw Tab Hygiene**: Standard Playwright wrapper contexts fail to track manually added or external tabs. Tab sweeping is now exclusively handled by `execution/raw_tab_sweep.py`, which circumvents wrapper-hallucination by natively requesting complete system JSON directly via HTTP to `localhost:9222/json/list` and enforcing single-viewport sterility.

## 🖥 Physical UI Mapping
- **Acer Monitor (Left Screen):** Dedicated strictly to the AMOS Browser. Viewport explicitly set to 986x963 upon startup. `amos_browser` automatically binds here.
- **LG TV (Right Screen):** Dedicated to the Anti-Gravity IDE and chat. All browser interactions are explicitly designed to harmoniously sync with this visual baseline.
- **Visual Override (`look.sh`):** If the subagent fails or we are actively conversing about a confusing visual block, the Master Assistant MUST execute `execution/look.sh` to grab a native screenshot of the monitor to immediately verify ground-truth reality.
